package edu.csumb.cgieg.mainmenu;

public class Librarian extends Customer { // librarian uses the same attributes as customer

    public Librarian(String aTransType, String aUsername, String aPassword, String aDateTime) {
        super(aTransType, aUsername, aPassword, aDateTime);
    }
}